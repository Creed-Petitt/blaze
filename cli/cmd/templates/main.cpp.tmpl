#include <blaze/app.h>

using namespace blaze;

int main() {
    App app;

    // Register Generated Controllers
    // controllers::UserController::register_routes(app);

    // Welcome Route
    app.get("/", []() -> Async<Json> {
        co_return Json({
            {"status", "ok"},
            {"message", "Welcome to Blaze!"}
        });
    });

    app.listen(8080);
    return 0;
}
