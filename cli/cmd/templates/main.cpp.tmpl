#include <blaze/app.h>

using namespace blaze;

int main() {
    App app;

    // Welcome Route
    app.get("/", []() -> Async<Json> {
        co_return Json({
            {"status", "ok"},
            {"message", "Welcome to Blaze!"}
        });
    });

    app.listen(8080);
    return 0;
}
